# code_repository has it's own gihub repo
code_repository/
# excluding data_repository as well because files are large and stored offline
data_repository/
# exclude NOTE.txt for containing sensitive info
NOTE.txt
# exclude figures
*.png
# exclude jupyter notebook checkpoints and cache
*.ipynb_checkpoints/
__pycache__/
